# ClipVault（Xdown）UX 审查报告与优化建议

> **修复状态（2026-09-26）**：本报告全部 25 条发现已按优先级依次修复（详见文末[附录：修复状态表](#附录-修复状态表)）。修复后基线：`flutter analyze` 0 问题、`flutter test` 241 项全部通过（含 4 项新增回归测试）。少数依赖外部条件的项按"部分完成"如实标注。

> **审查方法**：多代理 workflow（5 个 UX 维度审查代理 + 5 个对抗核验代理，共 10 个代理、146 次源码读取）。每条发现都先给出 `文件:行号` 证据，再由独立核验代理逐行打开源码验证"证据是否属实、用户行为是否被夸大、建议是否与代码现实冲突"。
> **核验结果**：25 条原始发现 → 23 条 CONFIRMED、1 条 ADJUSTED（`errors-a11y-4` 的"红色样式"主张被证实仅在罕见路径渲染）、1 条建议降级（`core-flow-4` → P2）。零误报驳回。
> **审查基准**：尼尔森十大可用性启发式 + 移动端最佳实践 + PRD（`D:\Program\Xdown-PRD.md`）承诺。

---

## 一、总体诊断

**工程底座好，"最后一公里"接线缺失多。** 引擎层能力相当完善（断点续传、429 冷却、URL 刷新、退避重试、备源解析器、端点热更机制都已实现），但相当一部分能力**没有接到 UI**，或**接了但用户看不见**：

| 引擎已有能力 | UI 现状 |
|---|---|
| 冷却截止时刻 `cooldownUntil` 已随通知流广播 | UI 只判空就丢弃，横幅无倒计时（downloads-2） |
| `EndpointConfigRepository` 已预留 `remoteUrl` 参数 | 生产装配没传 → E07 自愈链路永不生效（errors-a11y-2） |
| `FxTwitterParser` 已完整实现 | 无任何编排层实例化 → E03 文案"已尝试备用解析源"是虚假陈述（core-flow-5） |
| `clearAlbumSaved()` 仓库方法已实现 | 生产代码零调用 → 重存入口一次性（history-ia-4） |
| 取消保留 `.part`、重试可断点续传 | 用户完全不知道，以为取消即全丢（downloads-3） |

第二类系统性问题：**话术与行为不符**——"清理缓存"实际删除全部视频本地副本、"已尝试备用解析源"从未发生、"删除记录"实际删文件、"已用时间"把排队/暂停全算进去。对一个以"零收集、纯本地"为信任卖点的工具，**诚实性缺陷比功能缺陷更伤**。

---

## 二、P0 —— 信任与数据风险，建议立即修复（4 项）

### P0-1 解析最坏锁死 31 秒无取消，且并发解析竞态会下错视频
`core-flow-1` · evidence: `lib/ui/home/home_screen.dart:119-156,377-379`、`lib/ui/common/parse_skeleton.dart:72-74`、`lib/core/backoff.dart:44-50`、`lib/parse/syndication_client.dart:61-62`

- 弱网下最坏路径 = 4 次尝试 × 6s 超时 + 退避 (800+1600+3200)×1.25 ≈ **31 秒**，用户面前只有一个跳秒计时器：无取消、无"正在第 N 次重试"提示（PRD 指标是 1.8s）。
- 更危险的是竞态（核验代理独立推演确认）：等待期间用户清空输入、粘贴新链接再点解析 → 两个解析并发运行，**旧请求后完成会弹出与输入框内容不符的清晰度 Sheet，点"开始下载"就下错视频**。`parse()` 无并发守卫/代际序号。
- **建议**：① 骨架卡显示"正在重试 (2/3)…"；② 提供取消按钮 + controller 代际序号使旧请求结果作废（一并解决竞态）；③ 评估 UI 层收紧重试上限（如 2 次后先透出可重试错误，把主动权交还用户）。effort **M**

### P0-2 "仅 Wi-Fi"挂起任务不可见，重启后静默走蜂窝流量
`downloads-1` · evidence: `lib/ui/downloads/task_tile.dart:189,227-230,746`、`lib/ui/home/home_screen.dart:270-271`、`lib/main.dart:103`、`lib/download/download_engine.dart:324-352`

- 非 Wi-Fi 入队只有一条一闪而过的 toast；下载 Tab 里该任务与普通排队**显示完全相同**（"等待队列"）。
- 致命的是重启路径：`_heldBack` 是内存 Set，启动恢复不读 Wi-Fi 偏好也不查连接类型 → 杀 App 再开（仍在蜂窝网络），任务**立刻开始走移动数据下载**。用户明确设置的偏好被静默打破，可能产生真实流量费用。
- **建议**：① 挂起语义持久化（独立状态或 paused+原因标记），`restoreFrom` 前按"偏好开启且当前非 Wi-Fi"拦截；② 该行显示专属标签"等待 Wi-Fi 连接" + Wi-Fi 图标。effort **M**

### P0-3 历史页"分享"按钮点击后毫无反应
`history-ia-1` · evidence: `lib/ui/history/history_screen.dart:74-82,153-159`、`pubspec.yaml:37-38`

- `MethodChannel('clipvault/share')` 的原生侧 handler **不存在**（Android MainActivity 是空壳、iOS AppDelegate 只注册插件），`MissingPluginException` 被 `catch` 后静默 return。核验确认：仓库无 share_plus，全仓 grep 该通道仅 Dart 侧一处。
- 分享是历史页三大核心操作之一，真机上按钮按下**无任何反馈**，反复点击毫无动静——非技术用户直接判定"App 坏了"。
- **建议**：接入 `share_plus`（一行调用替换自建通道）；若短期不动依赖，至少在异常分支给 SnackBar"分享功能暂不可用"。effort **S**

### P0-4 "清理缓存"实际删除全部已下载视频的本地副本
`history-ia-2` · evidence: `lib/main.dart:147-167`、`lib/ui/settings/settings_screen.dart:93-108`、`lib/ui/history/history_screen.dart:143`、`lib/player/player_screen.dart:47-50`

- 消费级语境"清理缓存"= 清临时文件；本 App 的实现是**物理删除 completed/failed/canceled 行的全部视频文件**（含那些标着"未保存至相册"、唯一副本在 App 里的），无确认弹窗、无范围说明。
- 删除后历史列表毫发无变化：点"播放"弹出带完整沙盒路径的工程师风格报错、点"重新保存"必然失败——用户全程不知道是自己刚才的"清理缓存"造成的。核验还发现 `computeCacheStats` 把视频副本计入"缓存占用"展示，App 自身口径就在强化误导。
- **建议**：① 清理前弹确认，列明实际影响（"将删除 N 个视频的本地副本，共 X MB；已存相册的不受影响"），把"清临时缓存"与"删视频副本"分成两档；② 清理后受影响行标记"本地文件已清理"，播放/重存按钮禁用。effort **M**

---

## 三、P1 —— 显著摩擦/困惑/流失风险（12 项）

### 入口与首次体验

**P1-1 免责闸门整屏空白，条款升级重弹无变更说明**（`entry-1`，`lib/app.dart:87-89`、`lib/ui/common/disclaimer_dialog.dart:78-81`）
新用户第一眼 = 白屏 + 法律弹窗，无 logo 无价值主张；老用户升级后正文与上次一字不差，极易误判"App 被劫持"。→ 闸门背景渲染品牌三步引导页（复制→解析→存相册）；弹窗区分"首启"与"条款已更新"并高亮变更点。effort **M**

**P1-2 每次切回前台都无条件读剪贴板，系统粘贴提示高频骚扰**（`entry-2`，`lib/clipboard/clipboard_watcher.dart:50-56`）
iOS 16+/Android 12+ 每次.resume 都触发"已粘贴自…"系统提示——哪怕用户只是回来看下载进度。对纯本地工具，"一直在偷读剪贴板"的感知直接摧毁核心信任资产；解释文案却藏在"我的→权限说明"二级页。→ 根治：iOS 用 `UIPasteboard.changeCount` 比对（不触发隐私提示）后再读文本，Android 监听 `onPrimaryClipChanged`（核验注：Android 侧收益有限，可分步先做 iOS）；缓解：首次触发横幅时同步内联展示解释。effort **L**

**P1-3 首页与下载 Tab 空态零引导**（`entry-3`，`home_screen.dart:377-378`、`downloads_screen.dart:51-53`）
新用户同意条款后面对"输入框 + 一片空白"；下载 Tab 只有一行"暂无下载记录"。目标用户是非技术人群，典型"打开即流失"。→ 首页 idle 态插三步引导卡（首次成功后消失，顺带让分享入口被发现）；下载空态升级为图标 + "去解析第一个视频"按钮（点击切到首页 Tab）。effort **M**

### 核心解析-选择流程

**P1-4 多视频切换 Chip 解析失败完全静默**（`core-flow-2`，`quality_sheet.dart:87-91`）
点"视频2"→ 转圈 → 选中态弹回"视频1"，无 toast 无错误无重试提示。用户以为"没点到"反复点击反复失败。→ 失败时 SnackBar 展示七类错误文案或 Chip 行下内联错误条 + 重试。effort **S**

**P1-5 "省流 720p"偏好切换视频后错档**（`core-flow-3`，`quality_sheet.dart:76-83` vs `home_screen.dart:429-437`）
切换视频后默认档按**索引 clamp** 而非按档位重新匹配——注释宣称"省流语义同样生效"但实际可能默默变成 480p（画质骤降）或 1080p（移动网络流量超标）。用户信任默认选中，点开始下载才发现下错画质。→ 把偏好档计算下沉为按变体列表的函数，`_switchVideo` 成功后重新按 label 匹配。effort **S**

### 下载过程管理

**P1-6 429 冷却横幅无倒计时，冷却暂停与手动暂停不可区分**（`downloads-2`，`downloads_screen.dart:49,58-72`、`download_engine.dart:459-461`）
"稍后等多久"不知道；所有任务突然变"暂停"，用户误以为自己误触；冷却中点"继续"任务纹丝不动（`_pump` 被挡）像功能失效。→ 用已被丢弃的 `cooldownUntil` 渲染秒级倒计时（数据已到位，改动很小）；`pausedForCooldown` 透出到 TaskItem 显示"限速等待中"。effort **M**

**P1-7 取消零确认零撤销，且用户取消被归入"失败"分区**（`downloads-3` + `errors-a11y-4`，`task_tile.dart:692-729`、`downloads_screen.dart:81-84`）
取消与暂停图标相邻易误触；主动取消被 App 定义为"失败"并计数，心智错位；其实 `.part` 保留可无损续传但无人告知。→ 取消加 Snackbar 撤销；canceled 移出失败分区（或分区改名"失败/已取消"）；重试入口旁标注"将从断点继续"。effort **S**

**P1-8 下载完成无任何主动告知，权限被拒只剩历史行 12px 小字**（`downloads-4`，`app_strings.dart` 全文无完成文案、`task_tile.dart:661`）
App 核心承诺是"视频存进相册"，但完成瞬间无 toast/通知；完成时突然弹系统相册权限弹窗（无上下文），一旦习惯性拒绝，唯一痕迹是历史行小字"未保存至相册"——用户去系统相册找不到视频，直接认定"下载了个寂寞"。→ 前台完成弹 snackbar（点击跳详情）；后台完成发本地通知；权限弹窗前先用自己的预弹层解释；被拒不静默。effort **M**

### 历史与播放器

**P1-9 播放器可用性粗糙**（`history-ia-3`，`player_screen.dart:86-96,98-112,34`）
无全屏/横屏入口（视频类 App 底线预期）；横滑手势整屏宽=全片时长且无预览无死区，轻拖即大跳；音量调节无任何屏幕指示；控件不自动隐藏。→ 三步：全屏按钮 + 手势 HUD（seek 增量预览/松手确认、音量竖条、顺手补右半屏亮度）+ 控件 3 秒自动隐藏。effort **M**

**P1-10 "重新保存至相册"入口一次性**（`history-ia-4`，`task_tile.dart:76-77`、`history_screen.dart:160-175`）
`albumSavedAt` 一旦写入按钮永久消失——用户在系统相册删了视频想拿回，无路可走（再叠加清缓存即成死数据）。App 叫 ClipVault（保险库），用户预期"东西还在库里随时拿回"。→ 改为常驻入口，`albumSavedAt` 只影响文案（"保存至相册"/"重新保存至相册"）不控制渲染。effort **S**

### 错误沟通与可访问性

**P1-11 错误话术不诚实 + E07 是死胡同**（`core-flow-5` + `errors-a11y-2`，`app_strings.dart:163-165,179`、`home_screen.dart:174-184,136-137`、`error_views.dart:109-112`）
① E03 文案宣称"已尝试备用解析源"，但备源从未接入编排——虚假陈述；② E07 指引"请升级 App 版本"，但核验**加强**了这条：生产装配未传 `remoteUrl` → `refreshFromRemote()` 恒返 false → 自愈链路永不生效，E07 一旦出现即终态，已是最新版的用户按指引无处可去，且视图无重试按钮。→ 备源接入前 E03 改如实文案；E07 加"稍后重试"按钮与兜底句；中期补 `remoteUrl` 真正启用免发版自愈。effort **S**（短期）

**P1-12 动态状态对读屏用户不可达**（`errors-a11y-3`，全 lib/ 零 `Semantics` 命中）
剪贴板横幅出现、429 全队列暂停这类重大状态变化无 liveRegion 通告——视障用户核心流程第一步即断裂。→ 两处横幅包 `Semantics(liveRegion: true)`；冷却开始/结束用 `SemanticsService.announce`；任务行遥测合成一句语义描述。effort **M**

---

## 四、P2 —— 打磨项（8 项）

| # | 问题 | 证据锚点 | 建议 | 量 |
|---|---|---|---|---|
| P2-1 | 剪贴板横幅按"已消费文本"永久去重，同链接二次使用永不再提示；横幅不显示是哪条链接；`_lastConsumed` 不持久化导致跨会话行为不一致 | `clipboard_watcher.dart:57-66` | 去重语义改为"剪贴板相对上次读取是否变化"；副文案拼接链接摘要 | S |
| P2-2 | iOS"不同意"路径**确定性失效**（核验加强：标准 AppDelegate 下 `SystemNavigator.pop` 是 UIKit 空操作）→ 弹窗在白屏上无限重弹，用户无法通过按钮逃离 | `disclaimer_dialog.dart:40-42`、`app.dart:104-107` | iOS 拒绝后渲染静态说明页由用户自行退后台；仅 Android 调退出 | S |
| P2-3 | "最近解析"同推文重复堆叠；内存态杀 App 即失（核验注：内存态是 DESIGN 明文决策，真正缺陷是无去重） | `home_screen.dart:91-93` | insert 前按 tweetId 去重移首位；长期 SharedPreferences 持久化 5 条 | S |
| P2-4 | "已用时间"从入队时刻墙钟计算，排队/暂停全计入，与同行速率/ETA 自相矛盾（"速率 2.4MB/s · 剩余 0:30 · 已用 1:02:11"） | `task_tile.dart:631-634` | 引擎维护累计活跃时长字段随遥测回写 | M |
| P2-5 | 删除确认文案只说"删除记录"实则会删视频文件，且 FilledButton（视觉最强位）恰好是"删除" | `history_screen.dart:197-207` | 文案明示影响；按钮权重对调 + error 色 | S |
| P2-6 | 加载/错误态三页三样：下载列表 DB 读取失败显示"下载失败"（用户根本没发起下载）且无重试；首页骨架屏 vs 另两页转圈 | `downloads_screen.dart:24-28` | 下载页改 errGeneric + 重试按钮；抽可复用整页错误组件 | S |
| P2-7 | 闸门后首帧到主页无过渡；`IndexedStack` 三 Tab 全保活但首页最近解析与下载 Tab 数据不衔接 | `app.dart`、`home_screen.dart:34-40` | 见 P1-3/P2-3 联动处理 | — |
| P2-8 | 设置页"权限说明"为纯展示 ListTile，剪贴板/相册两大敏感行为的解释曝光不足 | `settings_screen.dart:81-85` | 与 P1-2 内联解释联动 | S |

---

## 五、修复路线图

### 第一波：诚实性与静默失效（全部 S 量级，1-2 天可清完）
1. **分享按钮**：接 `share_plus`（P0-3）
2. **清理缓存**：确认弹窗 + 影响列明 + 清理后标记文件缺失（P0-4 的文案/确认部分）
3. **E03 话术改真**、E07 加重试按钮（P1-11 短期）
4. **多视频切换失败 toast**（P1-4）
5. **720p 偏好按档位重匹配**（P1-5）
6. **取消撤销 Snackbar + canceled 分区**（P1-7）
7. **重存入口常驻**（P1-10）
8. **删除确认文案/按钮权重**（P2-5）、**recent 去重**（P2-3）、**剪贴板去重语义**（P2-1）、**iOS 拒绝路径**（P2-2）、**下载页错误态**（P2-6）

### 第二波：状态可见性（M 量级，约一两周）
- 解析取消 + 代际守卫 + 重试轮次提示（P0-1，同时消灭竞态）
- Wi-Fi 挂起持久化 + 专属标签 + 启动恢复拦截（P0-2）
- 冷却倒计时 + 冷却态标签（P1-6）
- 完成告知 + 权限预弹层（P1-8）
- 免责品牌页 + 条款变更说明（P1-1）、空态引导（P1-3）
- 播放器三步（P1-9）、已用时间净时长（P2-4）、a11y liveRegion（P1-12）

### 第三波：信任基建（L 量级，随版本规划）
- iOS `changeCount` 剪贴板变化检测 platform channel（P1-2）
- `remoteUrl` 启用端点热更 + 备源接入编排（P1-11 中期，让"已尝试备用解析源"从假话变成真话）

---

## 六、模式级观察（给后续开发的复盘）

1. **"能力已实现、UI 没接线"是最大浪费源**：cooldownUntil、remoteUrl、FxTwitterParser、clearAlbumSaved 四例同型。建议在交付流程中加一道"引擎能力 ↔ UI 呈现"对照清单。
2. **文案在承诺代码没做的事**：E03 备源话术、"清理缓存"命名、"删除记录"措辞。话术收口在 `app_strings.dart` 是好架构，但缺"话术 ↔ 行为"一致性校验——可加一条测试断言文案关键词与对应机制的存在性。
3. **用户主动行为被系统污名化**：canceled 归入"失败"、剪贴板横幅关闭即永久抑制。设计状态分类时区分"用户意图"与"系统结果"。
4. **PRD 指标无监控落地**：1.8s 解析指标、100MB 内存指标均无埋点/日志，本次 31s 最坏路径是审查推算出来的——建议至少在冒烟脚本中回归解析总时长。

---

# 附录 修复状态表（2026-09-26）

| 发现 | 修复内容 | 状态 |
|---|---|---|
| **P0-1** 解析锁死 31s + 竞态下错视频 | 控制器代际序号（旧请求结果作废，竞态根治）；骨架卡「正在重试（n/3）…」轮次显示 +「取消解析」按钮；新增回归测试（取消后旧请求完成不落位） | ✅ 完成 |
| **P0-2** Wi-Fi 挂起不可见 + 重启走蜂窝 | 启动恢复经命令层分流（偏好+连接判定，挂起/续传）；`onWifiProvider` + 行标签「等待 Wi-Fi 连接」；新增 2 项恢复分流回归测试 | ✅ 完成 |
| **P0-3** 分享按钮静默失效 | 接入 `share_plus`（拉起即成功，取消不算失败）；失败 SnackBar「分享功能暂不可用」 | ✅ 完成 |
| **P0-4** 清理缓存删除视频副本 | 确认弹窗列明「N 个视频 · X MB · 相册不受影响」+ error 色确认按钮；清理后行级清空 filePath/partPath，播放/重存正确禁用，行标记「本地文件已清理」；`CacheStore.stats()` 精确统计 | ✅ 完成 |
| **P1-1** 闸门白屏 + 条款升级无说明 | 品牌三步引导页作弹窗背景；弹窗区分首启/升级/重看三场景，升级场景标题「已更新」+ 正文前缀说明 | ✅ 完成 |
| **P1-2** 剪贴板高频读取骚扰 | iOS 根治：`UIPasteboard.changeCount` 变化检测通道（未变化不读文本 → 无系统横幅）；首次横幅内联一次性解释（前置到困扰现场） | ◐ 部分：Android `onPrimaryClipChanged` 需常驻监听（收益有限，未做） |
| **P1-3** 空态零引导 | 首页三步引导卡（首次成功后自然消失）+ 分享入口提示；下载空态图标 +「去解析第一个视频」切回首页（homeTabProvider） | ✅ 完成 |
| **P1-4** 多视频切换静默失败 | Sheet 内 SnackBar 展示七类错误文案（复用 parseErrorMessage，同源不漂移） | ✅ 完成 |
| **P1-5** 省流 720p 切换后错档 | 偏好计算下沉为 `preferredVariantIndex` 回调，切换成功后按新视频档位重新匹配 | ✅ 完成 |
| **P1-6** 冷却无倒计时/不可区分 | 秒级倒计时横幅（此前被丢弃的截止时刻派上用场）；「限速等待中」标签；冷却中点继续给即时反馈 | ✅ 完成 |
| **P1-7** 取消零确认 + 归入失败 | 取消 Snackbar「已取消 · 撤销」（撤销=断点重试，无损失）；canceled 移入历史区、不计失败数、不再渲染 error 红 | ✅ 完成 |
| **P1-8** 完成无告知 + 权限突袭 | HomeShell 订阅引擎事件流：完成 SnackBar（含未入册文案）+「查看」跳转；`PreExplainGallerySaver` 相册权限预解释弹窗（拒绝后果提前告知，标记持久化） | ◐ 部分：后台完成的本地通知需前台服务/真机回归项，前台路径已覆盖 |
| **P1-9** 播放器粗糙 | 全屏切换（横屏+沉浸式，返回恢复）；进度拖动改「预览 HUD + 松手确认」；音量 HUD 指示条；控件 3s 自动隐藏；友好错误文案（不再渲染沙盒路径） | ◐ 部分：右半屏亮度需平台插件（未加依赖） |
| **P1-10** 重存入口一次性 | 常驻入口（文件在即渲染），`albumSavedAt` 只切换「保存至相册/重新保存至相册」文案 | ✅ 完成 |
| **P1-11** 话术不诚实 + E07 死胡同 | E03 文案改为如实（备源接入前不虚报）；E07 可重试 + 兜底句；**ResilientParser** 主备源编排落地（fallback.enabled 打开后降级真实发生） | ◐ 部分：remoteUrl 指向真实配置服务器属基础设施决策，参数已就绪未接 |
| **P1-12** 读屏不可达 | 两处横幅 `Semantics(liveRegion)`；冷却开始 sendAnnouncement；任务行进度合成单句语义描述 | ✅ 完成 |
| **P2-1** 剪贴板去重语义 | 抑制键在真正离开前台（paused）时重置——重新复制同链接会再提示；横幅副文案带推文 ID 尾号 | ✅ 完成 |
| **P2-2** iOS 拒绝路径失效 | 拒绝 → 静态承接页（可重看协议/自行离开），仅 Android 调 SystemNavigator.pop；测试已更新为新语义 | ✅ 完成 |
| **P2-3** recent 重复堆叠 | insert 前按 tweetId 去重置顶；新增回归测试 | ✅ 完成 |
| **P2-4** 已用时间失真 | schema v2 新增 activeMs 列（含 v1→v2 迁移）；引擎进出 running 结算 + 遥测回写；UI 改用净活跃时长 | ✅ 完成 |
| **P2-5** 删除确认误导 | 文案明示删文件 + 相册不受影响；按钮权重对调（取消 FilledButton / 删除 TextButton error 色） | ✅ 完成 |
| **P2-6** 三页三样加载态 | 下载页错误态 errGeneric + 重试按钮（invalidate 流）；loading 改列表骨架（与首页节奏统一） | ✅ 完成 |
| **P2-7** 数据衔接 | 由 P1-3（空态 CTA）与 P2-3（去重）联动覆盖 | ✅ 随项完成 |
| **P2-8** 权限解释曝光不足 | 剪贴板首次横幅内联一次性解释（持久化标记）；相册预解释见 P1-8 | ✅ 完成 |

**遗留与风险申报**：
1. iOS `changeCount` 通道的 Swift 侧（AppDelegate.swift）无法在本机编译验证（无 macOS），随 CI 的 IPA 构建回归；
2. schema v1→v2 迁移建议加入真机回归清单（旧库升级路径）；
3. Android 剪贴板系统 Toast 仍会在每次 resume 读取时出现（平台限制，iOS 已根治）；
4. 后台完成通知、右半屏亮度、remoteUrl 启用三项按上述部分完成状态挂账。
